# Optional. Defaults to data/business_os.sqlite inside this app folder.
# Use a relative path for simple deployments, or an absolute path when your host provides persistent disk storage.
BUSINESS_OS_DB_PATH=data/business_os.sqlite

# Optional. Set to true during sales demos or screenshot capture to show a reset button.
BUSINESS_OS_DEMO_MODE=false
